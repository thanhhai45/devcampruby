<h1>Homepage</h1>
<p>Find me in app/views/pages/home.html.erb</p>

<table>
  <thead>
    <th>ID</th>
    <th>Title</th>
    <th>Bodies</th>
    <th>Created_At</th>
    <th>Updated_At</th>
  </thead>
  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><%= post.id %></td>
        <td><%= post.title %></td>
        <td><%= post.body %></td>
        <td><%= post.created_at %></td>
        <td><%= post.updated_at %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<%= @posts.inspect %>